#!/usr/bin/env node
/* Third catalogue extension: the suites, and the block tier.
 *
 * Meridian asked for comprehensive typography, e-commerce, visualisation, and
 * portal/screen suites, plus a tier of larger composed blocks — kept in a section
 * of its own rather than mixed into the component catalogue.
 *
 * The distinction matters and is the reason `blocks` is a separate category
 * rather than more components. A component is a primitive with one job and a
 * contract a library can hold. A block is an arrangement of components that
 * solves a recognisable product problem — a dashboard header, a checkout summary,
 * a player shell. Blocks are opinionated by design, and a design system that
 * cannot tell the two apart ends up shipping opinions as if they were primitives.
 *
 * Benchmarked against Tailwind UI, Mantine UI, Ant Design Pro, MUI Templates and
 * PrimeBlocks, which are where this tier exists in the ecosystems Crystal is
 * measured against.
 *
 *   node tools/extend-catalogue-3.cjs --write
 */
const fs = require('node:fs');
const path = require('node:path');

const DIR = path.resolve(__dirname, '../tokens/catalogue');
const WRITE = process.argv.includes('--write');

const R = 'Resin', H = 'Haze', F = 'Frost', S = 'Stone';
const c = (id, name, anatomy, states, material, geometry, semantics, crystal, product, parity) =>
  ({ id, name, anatomy, states, material, geometry, semantics, crystal, product, parity });

/* Shared shorthands: most entries in a suite share their material and geometry
   answers, and repeating them verbatim would invite them to drift apart. */
const readable = `${H} content fill on the surrounding material`;
const contentRadius = 'Content radius; spacing from the scale.';
const crisp = 'Text stays crisp: feathering is paint-only and never touches a glyph.';

const ADDITIONS = {
  '08-typography': [
    c('display', 'Display', 'The largest type step, for a single statement per view.',
      ['at-rest'], 'Inherits', 'Tightest tracking of the scale; one line where possible.',
      'Exactly one per view, and it is the h1 unless the page says otherwise.',
      'The step, tracking and leading', 'The words', ['mantine:Title', 'mui:Typography']),
    c('heading', 'Heading', 'Section headings, levels two to six.',
      ['at-rest'], 'Inherits', 'Step per level; leading tightens as size grows.',
      'The level is the document structure, never chosen for size — size comes from the step.',
      'The scale and its tracking', 'Level and wording', ['mantine:Title', 'antd:Typography.Title']),
    c('lead', 'Lead', 'The standfirst paragraph beneath a title.',
      ['at-rest'], 'Inherits', 'One step above body; measure capped for reading.',
      'A paragraph, not a heading.', 'Step and measure', 'The words', ['mantine:Text']),
    c('prose-list', 'Prose list', 'Ordered and unordered lists inside running text, with Crystal markers.',
      ['at-rest'], 'Inherits', 'Marker inset follows the spacing scale; hanging indent preserved.',
      'Real ul and ol so the count and nesting are announced.',
      'Marker treatment, spacing, nesting rhythm', 'Content and ordering',
      ['mantine:List', 'antd:Typography']),
    c('code-block', 'Code block', 'A multi-line code sample with optional highlighting.',
      ['at-rest', 'copied', 'focus-visible'], `${H} fill with a crisp monospace face`,
      'Content radius; the copy control is a pill.',
      'Focusable and scrollable by keyboard; the language is announced; the copy button says what it copied.',
      'Fill material, the monospace pairing, copy feedback motion',
      'Which highlighter, and the languages', ['mantine:CodeHighlight', 'primereact:editor']),
    c('truncate', 'Truncate', 'Clamps text to a line count with an accessible full value.',
      ['clamped', 'expanded'], 'Inherits', 'Clamp by line count, never by pixel height.',
      'The full text remains available to assistive technology; expansion is a real control.',
      'The fade, if any, and the expand motion', 'Line count and expansion policy',
      ['mantine:Spoiler', 'antd:Typography.Paragraph']),
    c('cite', 'Cite', 'An attribution attached to a quotation.',
      ['at-rest'], 'Inherits', 'Sits beneath the quote at one step down.',
      'Uses cite, and is associated with its blockquote.',
      'Step and colour', 'The attribution', ['mantine:Blockquote']),
    c('abbreviation', 'Abbreviation', 'An abbreviation with its expansion available.',
      ['at-rest', 'focus-visible'], 'Inherits', 'No visual weight change; underline on hover only.',
      'Uses abbr with a title, and is focusable so the expansion is reachable without a pointer.',
      'The underline treatment', 'The expansion text', ['antd:Typography.Text']),
    c('text-balance', 'Balanced text', 'Headings wrapped so lines are even rather than ragged.',
      ['at-rest'], 'Inherits', 'Balances wrapping without changing the step.',
      'Purely visual: the text content is unchanged.',
      'Where balancing applies, and its fallback', 'The words', ['mantine:Title']),
    c('gradient-text', 'Gradient text', 'Text filled with a palette gradient.',
      ['at-rest'], 'Palette gradient clipped to the glyphs',
      'No change to step or tracking.',
      'Contrast is checked against the surface at both gradient ends, and a solid fallback applies where the clip is unsupported.',
      'The gradient, and the contrast floor at every point',
      'Which text warrants it', ['mantine:Text']),
  ],

  '05-data-display': [
    c('stat-card', 'Stat card', 'A single figure with its label, trend and period.',
      ['at-rest', 'loading', 'empty'], readable, contentRadius,
      'The figure and its trend are one readable sentence, not a number beside an arrow.',
      'Material, figure typography, trend colour from the status tokens', 'The data and its period',
      ['mantine:Card', 'antd:Statistic']),
    c('kpi-tile', 'KPI tile', 'A stat card with a target and progress toward it.',
      ['at-rest', 'on-target', 'off-target', 'loading'], readable, contentRadius,
      'Target attainment is stated in words as well as shown.',
      'Material, progress rendering, status colour', 'Targets and thresholds',
      ['antd:Statistic', 'primereact:card']),
    c('trend-indicator', 'Trend indicator', 'Direction and magnitude of a change.',
      ['up', 'down', 'flat'], 'Inherits', 'Sits inline with the figure it qualifies.',
      'Direction is carried by a word and a symbol, never by colour alone.',
      'Status colour, symbol pairing', 'What counts as a meaningful change',
      ['antd:Statistic', 'mantine:Text']),
    c('delta-badge', 'Delta badge', 'A compact signed change.',
      ['positive', 'negative', 'neutral'], `${H} fill`, 'Pill.',
      'The sign is a character, not a colour.', 'Status tokens, pill geometry',
      'Formatting and precision', ['antd:Tag']),
  ],

  '10-charts': [
    c('calendar-heatmap', 'Calendar heatmap', 'Daily values across weeks and months.',
      ['at-rest', 'hover', 'focus-visible', 'empty'], 'Inherits the chart surface',
      'Square cells with a hairline gap; week and month labels outside.',
      'Every cell states its date and value; intensity is paired with a number.',
      'Scale construction and the contrast floor at every step', 'Data and bucketing',
      ['mui-x-charts:Heatmap']),
    c('treemap', 'Treemap', 'Nested proportions as nested rectangles.',
      ['at-rest', 'hover', 'focus-visible', 'empty'], 'Inherits the chart surface',
      'Hairline separation; labels drop out rather than overflow.',
      'Navigable as a tree, with each node stating its share.',
      'Nesting depth rendering, label policy', 'Hierarchy and values',
      ['recharts:Treemap']),
    c('sankey', 'Sankey', 'Flow between stages, with width as volume.',
      ['at-rest', 'hover', 'focus-visible', 'empty'], 'Inherits the chart surface',
      'Link opacity keeps crossings readable.',
      'Each link states its endpoints and volume as text.',
      'Link material and opacity so overlaps stay legible', 'Nodes and flows',
      ['recharts:Sankey']),
    c('candlestick-chart', 'Candlestick chart', 'Open, high, low and close per period.',
      ['at-rest', 'hover', 'focus-visible', 'empty'], 'Inherits the chart surface',
      'Wick and body share one x centre; body has no radius.',
      'Each period is reachable and states all four values.',
      'Rise and fall colour from the status tokens, never red and green alone',
      'Data and period', ['mui-x-charts:BarChart']),
    c('waterfall-chart', 'Waterfall chart', 'Cumulative effect of sequential changes.',
      ['at-rest', 'hover', 'focus-visible', 'empty'], 'Inherits the chart surface',
      'Connectors align with bar edges.',
      'Each step states its delta and the running total.',
      'Increase, decrease and total colour from the status tokens', 'Steps and values',
      ['mui-x-charts:BarChart']),
    c('bullet-chart', 'Bullet chart', 'A measure against a target and qualitative ranges.',
      ['at-rest', 'focus-visible', 'empty'], `${H} range bands`,
      'Single row; the target is a crisp marker, not a bar.',
      'role="meter" with the target stated in text.',
      'Band materials, marker crispness', 'Ranges and targets', ['mui-x-charts:Gauge']),
    c('box-plot', 'Box plot', 'Distribution summary: quartiles, median and outliers.',
      ['at-rest', 'hover', 'focus-visible', 'empty'], 'Inherits the chart surface',
      'Whisker caps align with the box width.',
      'Each summary statistic is reachable as text; outliers are counted, not only drawn.',
      'Box and whisker geometry', 'Data and outlier rule', ['recharts:Scatter']),
    c('histogram', 'Histogram', 'Frequency across bins.',
      ['at-rest', 'hover', 'focus-visible', 'empty'], 'Inherits the chart surface',
      'Bins meet without gaps; the value end keeps a small radius.',
      'Bin bounds and counts are text.', 'Bin geometry, series colour',
      'Bin width and range', ['mui-x-charts:BarChart']),
    c('geo-map', 'Geographic map', 'Values by region on a projected map.',
      ['at-rest', 'hover', 'focus-visible', 'empty'], 'Inherits the chart surface',
      'Region borders are hairlines in the rim colour.',
      'Regions are reachable by keyboard and state their name and value; a table equivalent is required.',
      'Scale construction, border treatment, contrast floor',
      'Projection, topology, and the data join', ['recharts:Customized']),
    c('network-graph', 'Network graph', 'Nodes and the edges between them.',
      ['at-rest', 'hover', 'focus-visible', 'empty'], `${H} node fills`,
      'Node size is a scale; edges are hairlines.',
      'Nodes are reachable by keyboard; each states its degree and neighbours.',
      'Node material, edge treatment, focus ring on a node',
      'Layout algorithm and data', ['primereact:organizationchart']),
  ],

  '12-commerce': [
    c('price', 'Price', 'A monetary amount with its currency and locale.',
      ['at-rest'], 'Inherits', 'Tabular figures so a column of prices aligns.',
      'The formatted value is the text content; currency is stated, not implied by a symbol alone.',
      'Figure style and step', 'Currency, locale, precision, tax display',
      ['antd:Statistic']),
    c('price-range', 'Price range', 'A from-to price, or a from price.',
      ['at-rest'], 'Inherits', 'Matches price.',
      'Reads as a sentence rather than two numbers with a dash.',
      'Typography', 'Range rules', ['antd:Statistic']),
    c('discount-badge', 'Discount badge', 'A reduction, as an amount or a percentage.',
      ['at-rest'], `${H} fill with a status accent`, 'Pill.',
      'States what is reduced from what; a percentage alone is not a claim.',
      'Status tokens, pill geometry', 'Discount rules and rounding', ['antd:Tag']),
    c('quantity-stepper', 'Quantity stepper', 'A bounded integer with decrement and increment.',
      ['at-rest', 'focus-visible', 'at-min', 'at-max', 'disabled'], `${R} shell with a ${H} well`,
      'Pill; both controls reach 44px.',
      'A spin button: the value is typable, and the bounds are announced when reached.',
      'Materials, press recipe on each control', 'Bounds, step, stock rules',
      ['react-aria:NumberField', 'primereact:inputnumber']),
    c('variant-selector', 'Variant selector', 'Choice among product variants, with availability.',
      ['at-rest', 'selected', 'unavailable', 'focus-visible'], `${H} option fills`,
      'Options are pills; swatch variants are circles.',
      'Unavailable options stay perceivable and say why; selection is label weight, never a check mark.',
      'Option material, selection weight, unavailable treatment',
      'Variant axes and stock', ['mantine:SegmentedControl']),
    c('stock-indicator', 'Stock indicator', 'Availability, with urgency where it applies.',
      ['in-stock', 'low', 'out-of-stock', 'backorder'], 'Inherits',
      'Inline with the price or the action.',
      'Words carry the state; status colour reinforces it.',
      'Status tokens', 'Thresholds and wording', ['antd:Tag']),
    c('product-card', 'Product card', 'One product: media, name, price, rating and a primary action.',
      ['at-rest', 'hover', 'focus-visible', 'unavailable'], readable, contentRadius,
      'The whole card is not a link; the name is, and the action is a button. One tab stop each.',
      'Material, media aspect, action geometry, hover recipe',
      'Which attributes appear', ['primereact:card', 'mantine:Card']),
    c('product-gallery', 'Product gallery', 'Product media with thumbnails and zoom.',
      ['at-rest', 'zoomed', 'focus-visible'], `${H} thumbnails; Mirage scrim when enlarged`,
      'Thumbnails keep the content radius; the viewer is full-bleed within the scrim.',
      'Arrow keys move between items with position announced; zoom is keyboard reachable.',
      'Scrim and thumbnail materials, the transition into the viewer',
      'The media set', ['primereact:galleria']),
    c('cart-item', 'Cart item', 'One line of a basket: product, quantity, price, remove.',
      ['at-rest', 'updating', 'removed', 'focus-visible'], readable, contentRadius,
      'Removal is announced and undoable; quantity changes announce the new subtotal.',
      'Material, the update and remove motion', 'Pricing and stock rules',
      ['antd:List.Item']),
    c('cart-summary', 'Cart summary', 'Subtotal, discounts, shipping, tax and total.',
      ['at-rest', 'updating', 'empty'], readable, contentRadius,
      'A description list, so each line is a labelled pair; the total is marked as such.',
      'Material, figure alignment, the motion of a changing total',
      'What the lines are and how they are computed', ['antd:Descriptions']),
    c('coupon-input', 'Coupon input', 'A code field with apply, validation and applied state.',
      ['at-rest', 'focus-visible', 'applying', 'applied', 'invalid'], `${H} well in a ${R} shell`,
      'Field and action share one pill row.',
      'Success and failure are announced; an applied code is removable.',
      'Materials, field-valid and field-invalid recipes', 'Validation and stacking rules',
      ['antd:Input.Search']),
    c('checkout-steps', 'Checkout steps', 'Progress through a purchase.',
      ['at-rest', 'current', 'complete', 'error', 'focus-visible'], `${H} step fills`,
      'Steps are pills; connectors are hairlines.',
      'The current step is aria-current; completion is stated in words, and a check mark here means validated rather than selected.',
      'Step materials, connector treatment, the advance motion',
      'The steps and their validation', ['antd:Steps', 'mui:Stepper']),
    c('payment-method', 'Payment method', 'Choice among stored or new payment methods.',
      ['at-rest', 'selected', 'invalid', 'focus-visible'], `${H} option fills`,
      'Options keep the content radius; the selected one carries label weight.',
      'A radio group. Card fields are never reimplemented — the host supplies its provider element.',
      'Option material, selection, focus ring',
      'The provider, tokenisation, and PCI scope — this component never handles raw card data',
      ['antd:Radio.Group']),
    c('address-form', 'Address form', 'A locale-aware address, with the right fields per country.',
      ['at-rest', 'validating', 'invalid', 'submitting'], `${H} wells in ${R} shells`,
      'Fields follow the input geometry.',
      'Field order, labels and required-ness change by country; postcode validation is per-locale, not one regular expression.',
      'Materials, validation recipes', 'Countries supported and their field rules',
      ['antd:Form']),
    c('order-summary', 'Order summary', 'A completed or pending order, with items and totals.',
      ['at-rest', 'pending', 'shipped', 'cancelled'], readable, contentRadius,
      'Status is a word; the status colour reinforces it.',
      'Material, status tokens', 'Order model and statuses', ['antd:Descriptions']),
    c('shipping-selector', 'Shipping selector', 'Delivery options with price and estimate.',
      ['at-rest', 'selected', 'unavailable', 'focus-visible'], `${H} option fills`,
      'Options keep the content radius.',
      'A radio group; each option states price and estimate together.',
      'Option material, selection weight', 'Rates and estimates',
      ['antd:Radio.Group']),
    c('delivery-estimate', 'Delivery estimate', 'When something will arrive.',
      ['at-rest', 'loading', 'unavailable'], 'Inherits', 'Inline.',
      'An absolute date, not only a relative phrase.',
      'Typography and colour', 'Estimation and locale', ['antd:Typography']),
    c('wishlist-button', 'Wishlist button', 'Adds or removes an item from a saved list.',
      ['at-rest', 'saved', 'pressed', 'focus-visible'], `${R} pill or icon button`,
      'Pill; 44px target.',
      'A toggle with a pressed state and a name that says what it will do.',
      'Press recipe, the state-change recipe', 'Persistence', ['mantine:ActionIcon']),
    c('review', 'Review', 'One review: rating, title, body, author and date.',
      ['at-rest', 'expanded'], readable, contentRadius,
      'The rating is text as well as stars.',
      'Material, rating rendering, expansion motion', 'Moderation and sorting',
      ['primereact:rating']),
    c('rating-summary', 'Rating summary', 'Average rating with a distribution.',
      ['at-rest', 'empty'], `${H} distribution bars`, contentRadius,
      'The average and the count are both stated; bars are labelled.',
      'Bar material, figure typography', 'How the average is computed',
      ['primereact:rating']),
    c('filter-panel', 'Filter panel', 'Faceted filters with counts and clear-all.',
      ['at-rest', 'applied', 'loading', 'focus-visible'], `${F} panel with ${H} groups`,
      'Panel radius; controls are pills.',
      'Applied filters are announced and individually removable; counts update politely.',
      'Panel and group materials, the apply and clear motion',
      'The facets and their counts', ['antd:Collapse', 'primereact:accordion']),
    c('sort-select', 'Sort select', 'Ordering for a product list.',
      ['at-rest', 'focus-visible', 'open'], `${H} well in a ${R} shell`, 'Pill.',
      'A select whose current ordering is announced on change.',
      'Materials, open motion', 'The orderings offered', ['antd:Select']),
    c('compare-table', 'Compare table', 'Products side by side by attribute.',
      ['at-rest', 'focus-visible'], 'Inherits the table surface',
      'Sticky first column and header; differences are marked, not only coloured.',
      'A real table with row and column headers; differences are stated in text.',
      'Table surface, sticky treatment, difference marking',
      'Which attributes compare', ['antd:Table']),
    c('recently-viewed', 'Recently viewed', 'A horizontal strip of previously seen products.',
      ['at-rest', 'empty', 'focus-visible'], 'Inherits from its cards',
      'Scrolls within its own container; never the page.',
      'A labelled list; the strip is keyboard scrollable.',
      'Scroll surface, edge fade from the surrounding material',
      'Retention and privacy policy', ['primereact:carousel']),
  ],

  '13-screens': [
    c('screen', 'Screen', 'A full view with its own header, content region and chrome.',
      ['at-rest', 'loading', 'error'], 'Plastic foundation with Frost chrome',
      'Full-bleed; safe areas respected.',
      'One main per view; the heading is the view name.',
      'Region materials, elevation ordering, safe-area handling',
      'Routing and what the regions hold', ['antd:Layout', 'mui:Container']),
    c('page-header', 'Page header', 'Title, description, breadcrumbs and page actions.',
      ['at-rest', 'condensed'], `${F} band or inherits`, 'Actions are pills.',
      'Contains the h1; breadcrumbs are a navigation landmark.',
      'Material, condense behaviour on scroll', 'Actions and copy',
      ['antd:PageHeader', 'mantine:Group']),
    c('view-stack', 'View stack', 'Pushed views with a back affordance.',
      ['at-rest', 'pushing', 'popping'], 'Inherits per view',
      'Views enter and leave along the reading direction.',
      'Focus moves to the new view and returns on pop; the back control is a real button.',
      'Enter and exit recipes, focus management', 'The stack and routing',
      ['react-aria:RouterProvider']),
    c('master-detail', 'Master detail', 'A list beside the detail of its selection.',
      ['at-rest', 'empty-selection', 'narrow'], `${F} list pane; ${H} detail surface`,
      'Collapses to a stack below the layout breakpoint.',
      'Selection moves focus to the detail only when the layout has collapsed.',
      'Pane materials, the collapse transition', 'The data and routing',
      ['antd:Layout']),
    c('split-view', 'Split view', 'Two resizable regions.',
      ['at-rest', 'resizing', 'collapsed', 'focus-visible'], 'Inherits per pane',
      'The divider is a 44px target.',
      'The divider is a separator with a value, resizable by keyboard.',
      'Divider material, focus ring', 'Panes and persistence',
      ['primereact:splitter', 'antd:Splitter']),
    c('command-bar', 'Command bar', 'A context-sensitive row of actions for the current view.',
      ['at-rest', 'focus-visible', 'overflowing'], `${R} plane`, 'Pill; overflow to a menu.',
      'role="toolbar" with one tab stop.',
      'Material, overflow behaviour, press recipes', 'Which commands apply',
      ['mui:Toolbar']),
    c('status-bar', 'Status bar', 'Persistent status at the edge of a view.',
      ['at-rest', 'busy', 'error'], `${S} backing`, 'Full-bleed; text stays crisp.',
      'role="status"; errors escalate to assertive.',
      'Material, status tokens', 'What is reported', ['antd:Layout.Footer']),
    c('workspace', 'Workspace', 'A persistent multi-pane working area.',
      ['at-rest', 'focus-mode'], 'Plastic foundation with Frost panels',
      'Panels keep panel radius.',
      'Each pane is a labelled region; focus order follows the visual order.',
      'Panel materials, pane transitions', 'The panes and their layout',
      ['antd:Layout']),
    c('focus-mode', 'Focus mode', 'Hides chrome to leave one task visible.',
      ['off', 'on', 'transitioning'], 'Inherits', 'Chrome leaves along its own edge.',
      'Entering and leaving are announced; nothing becomes unreachable, only hidden.',
      'The enter and exit recipes', 'What counts as chrome',
      ['mantine:AppShell']),
    c('empty-screen', 'Empty screen', 'A whole view with nothing in it yet.',
      ['at-rest'], readable, contentRadius,
      'Says what would be here and offers the action that creates it.',
      'Material, illustration treatment', 'The copy and the action',
      ['antd:Empty', 'mantine:Center']),
    c('error-screen', 'Error screen', 'A view that failed, with a way forward.',
      ['at-rest'], readable, contentRadius,
      'role="alert"; states what failed and what to try, never only a code.',
      'Material, status tokens', 'The error taxonomy', ['antd:Result']),
    c('loading-screen', 'Loading screen', 'A whole view still arriving.',
      ['at-rest'], 'Skeletons on the surrounding material',
      'Skeletons match the shape of what is coming.',
      'aria-busy on the region; the wait is announced once, not repeatedly.',
      'Skeleton material and the reduced-motion contract', 'What is loading',
      ['antd:Skeleton', 'mantine:Skeleton']),
    c('offline-screen', 'Offline screen', 'Connectivity lost, with what still works.',
      ['at-rest', 'reconnecting'], readable, contentRadius,
      'Announced politely; retry is a real button.',
      'Material, status tokens', 'What is available offline', ['antd:Result']),
    c('not-found-screen', 'Not found screen', 'The requested thing does not exist.',
      ['at-rest'], readable, contentRadius,
      'States what was not found and offers a route onward.',
      'Material', 'The copy and the routes', ['antd:Result']),
    c('permission-screen', 'Permission screen', 'Access is denied or must be granted.',
      ['denied', 'requesting'], readable, contentRadius,
      'Says which permission and why; the request is a real button.',
      'Material, status tokens', 'The permission model', ['antd:Result']),
  ],

  '14-blocks': [
    c('dashboard-shell', 'Dashboard shell', 'Header, navigation rail, content grid and status bar.',
      ['at-rest', 'collapsed', 'narrow'], 'Plastic foundation, Frost chrome, Resin actions',
      'Panel radius throughout; safe areas respected.',
      'Landmarks in place: banner, navigation, main, contentinfo. One main.',
      'Region materials and elevation ordering', 'Navigation model and routing',
      ['antd-pro:ProLayout', 'mui-templates:Dashboard']),
    c('metrics-row', 'Metrics row', 'A row of KPI tiles that reflows by container width.',
      ['at-rest', 'loading', 'empty'], 'Inherits from its tiles', 'Gap from the scale.',
      'A labelled list of figures.', 'Reflow behaviour', 'Which metrics',
      ['antd-pro:StatisticCard']),
    c('analytics-panel', 'Analytics panel', 'A chart with its controls, legend and range picker.',
      ['at-rest', 'loading', 'empty', 'error'], `${H} panel on the surrounding material`,
      contentRadius, 'The chart carries a table equivalent; controls are real controls.',
      'Panel material, control geometry', 'The data and the ranges',
      ['antd-pro:ChartCard', 'mui-templates:Analytics']),
    c('data-table-block', 'Data table block', 'A table with toolbar, filters, bulk actions and pagination.',
      ['at-rest', 'loading', 'empty', 'selecting', 'error'], 'Table surface with Frost overlays',
      'Toolbar controls are pills; the table keeps its own surface.',
      'Selection count is announced; bulk actions describe what they will affect.',
      'Surfaces, selection treatment, the motion of entering bulk mode',
      'Data, filters, and what the actions do', ['antd-pro:ProTable', 'mui-x-data-grid:DataGrid']),
    c('crud-form-block', 'CRUD form block', 'A create or edit form with sections, validation and submit.',
      ['at-rest', 'validating', 'submitting', 'saved', 'error'], `${H} sections in a ${F} panel`,
      contentRadius, 'Errors summarise at the top and link to their fields; submission state is announced.',
      'Section materials, field recipes, the submit transition',
      'The schema and the mutation', ['antd-pro:ProForm']),
    c('settings-block', 'Settings block', 'Grouped preferences with immediate or deferred save.',
      ['at-rest', 'dirty', 'saving', 'saved'], `${H} groups`, contentRadius,
      'Each group is a labelled region; unsaved changes are announced before navigation.',
      'Group material, the save transition', 'Which settings, and when they apply',
      ['antd-pro:ProForm']),
    c('auth-block', 'Auth block', 'Sign in, register, reset and verification.',
      ['at-rest', 'submitting', 'error', 'locked'], `${H} card on the Plastic foundation`,
      contentRadius,
      'Real form semantics with autocomplete tokens; failures never reveal which factor was wrong.',
      'Card material, field recipes', 'The auth provider and its policy',
      ['mui-templates:SignIn', 'mantine-ui:Authentication']),
    c('profile-block', 'Profile block', 'Identity, avatar, contact details and account actions.',
      ['at-rest', 'editing', 'saving'], readable, contentRadius,
      'Destructive actions confirm and say what they remove.',
      'Material, avatar geometry', 'The profile model', ['antd-pro:ProDescriptions']),
    c('activity-feed', 'Activity feed', 'Chronological events with actors and actions.',
      ['at-rest', 'loading', 'empty', 'live'], `${H} rows`, contentRadius,
      'Live updates are announced politely and never steal focus.',
      'Row material, the insert motion', 'The event model',
      ['antd:Timeline', 'mantine:Timeline']),
    c('notification-centre', 'Notification centre', 'Grouped notifications with read state and actions.',
      ['at-rest', 'unread', 'empty', 'focus-visible'], `${F} panel with ${H} rows`,
      'Panel radius; rows reach 44px.',
      'Unread count is announced; marking read is undoable.',
      'Panel and row materials, the read transition', 'The notification model',
      ['antd:Notification']),
    c('player-shell', 'Player shell', 'A media surface with transport, queue and metadata.',
      ['idle', 'playing', 'paused', 'buffering', 'full-screen'],
      `${R} transport over the media; ${H} metadata`,
      'Transport is a pill; controls reach 44px.',
      'Real media elements; captions and their state announced; transport reachable by keyboard.',
      'Transport material, the show and hide motion', 'Sources, queue, DRM',
      ['primereact:galleria']),
    c('playlist-block', 'Playlist block', 'An ordered, reorderable queue with now-playing state.',
      ['at-rest', 'playing', 'dragging', 'focus-visible'], `${H} rows`, contentRadius,
      'Reorder works by keyboard; the now-playing row is aria-current.',
      'Row material, the drag lift and settle', 'The queue model',
      ['react-aria:GridList']),
    c('storefront-block', 'Storefront block', 'Product grid with filters, sort and pagination.',
      ['at-rest', 'loading', 'empty', 'filtering'], 'Inherits from its cards and panels',
      'Grid gap from the scale; reflows by container width.',
      'Result count is announced on filter change; the grid is a labelled list.',
      'Card and panel materials, the filter transition', 'Catalogue and facets',
      ['primeblocks:ecommerce']),
    c('product-detail-block', 'Product detail block', 'Gallery, variants, price, stock and purchase action.',
      ['at-rest', 'unavailable', 'adding'], 'Inherits from its parts', contentRadius,
      'Variant changes update price and stock together, and say so.',
      'Composition of materials, the add-to-cart transition',
      'The product model', ['primeblocks:ecommerce']),
    c('checkout-block', 'Checkout block', 'Steps, address, shipping, payment and the order summary.',
      ['at-rest', 'validating', 'submitting', 'error'], `${H} sections in a ${F} panel`,
      contentRadius,
      'Each step is a labelled region; errors summarise and link; raw card data never touches this component.',
      'Section materials, step transitions', 'Providers, tax, and the order model',
      ['primeblocks:ecommerce', 'mui-templates:Checkout']),
    c('cart-drawer', 'Cart drawer', 'The basket as a drawer, with items, totals and checkout.',
      ['closed', 'open', 'updating', 'empty'], `${F} drawer with ${H} rows`,
      'Drawer keeps panel radius on its inner edge.',
      'Opening moves focus in and returns it on close; total changes are announced.',
      'Drawer material, the enter and exit recipes', 'The cart model',
      ['antd:Drawer']),
    c('pricing-block', 'Pricing block', 'Plan comparison with features and a call to action.',
      ['at-rest', 'recommended', 'focus-visible'], readable, contentRadius,
      'The recommended plan is stated in words, not only styled.',
      'Card materials, emphasis treatment', 'Plans and their features',
      ['mantine-ui:Pricing']),
    c('onboarding-block', 'Onboarding block', 'A guided first-run sequence with progress.',
      ['at-rest', 'active', 'complete', 'skipped'], `${F} panel over a Mirage scrim`,
      'Panel radius; the highlight follows the target radius.',
      'Escapable at every step; progress announced; focus moves with the step.',
      'Panel and scrim materials, step motion, focus management',
      'The steps and their copy', ['antd:Tour']),
    c('search-block', 'Search block', 'Search with suggestions, recent queries and results.',
      ['at-rest', 'open', 'loading', 'empty', 'focus-visible'], `${F} overlay with ${H} rows`,
      'Overlay keeps panel radius; rows reach 44px.',
      'A combobox: results are announced by count, and arrow keys move without losing the typed value.',
      'Overlay material, the open motion, row treatment',
      'The search backend and ranking', ['mantine:Spotlight', 'primereact:autocomplete']),
    c('editor-block', 'Editor block', 'A rich text surface with toolbar, and save state.',
      ['at-rest', 'focus-visible', 'dirty', 'saving', 'saved'], `${H} surface in a ${F} frame`,
      'Toolbar controls are pills; the surface keeps the content radius.',
      'The toolbar is a real toolbar with one tab stop; formatting state is announced.',
      'Surface and toolbar materials, the save transition',
      'The editor engine and its schema', ['primereact:editor', 'mantine:RichTextEditor']),
  ],
};

const META = {
  '12-commerce': {
    id: 'commerce',
    name: 'Commerce',
    description: 'Functional e-commerce components. Crystal named none before, which left every store to reinvent a quantity stepper, a variant selector and a cart summary — and to re-solve availability, currency formatting and checkout validation each time. Payment is deliberately bounded: these components never handle raw card data, and defer to the host provider\'s own element.',
    components: [],
  },
  '13-screens': {
    id: 'screens',
    name: 'Screens',
    description: 'Whole views and the chrome around them, for interactive applications rather than documents. A screen owns landmarks, focus movement between views, and the states a view can be in before it has content — loading, empty, error, offline, not found, unauthorised — which products otherwise improvise separately and inconsistently.',
    components: [],
  },
  '14-blocks': {
    id: 'blocks',
    name: 'Blocks',
    description: 'Composed arrangements that solve a recognisable product problem: a dashboard shell, a checkout, a player, a storefront. Kept as their own tier rather than mixed into the components, because a component is a primitive with one job and a contract, while a block is opinionated by design — and a design system that cannot tell the two apart ships opinions as if they were primitives. Benchmarked against Tailwind UI, Mantine UI, Ant Design Pro, MUI Templates and PrimeBlocks.',
    components: [],
  },
};

let added = 0;
const report = [];

for (const [file, entries] of Object.entries(ADDITIONS)) {
  const full = path.join(DIR, `${file}.json`);
  let doc;
  if (fs.existsSync(full)) doc = JSON.parse(fs.readFileSync(full, 'utf8'));
  else if (META[file]) doc = { ...META[file] };
  else { console.error(`No catalogue file ${file}.json and no template`); process.exit(1); }

  const have = new Set(doc.components.map((x) => x.id));
  const fresh = entries.filter((x) => !have.has(x.id));
  if (!fresh.length) continue;
  doc.components.push(...fresh);
  added += fresh.length;
  report.push(`${file}: +${fresh.length} -> ${doc.components.length}`);
  if (WRITE) fs.writeFileSync(full, JSON.stringify(doc, null, 2) + '\n');
}

if (!added) { console.log('The catalogue already carries every addition.'); process.exit(0); }
console.log(`${added} components to add:`);
for (const line of report) console.log('  ' + line);
console.log(WRITE ? '\nWritten. Run tools/build-catalogue.cjs.' : '\nRe-run with --write to apply.');
